import { HypeBand } from "@four/ui";

/**
 * The full-bleed red statement band. Branch names and addresses come from the
 * shared BRANCHES data, so this is the real three-kitchen content. Shown
 * settled - the preview provider forces reduced motion, so the scroll-in
 * animations are already resolved.
 */
export const Default = () => <HypeBand />;
