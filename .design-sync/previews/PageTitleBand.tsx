import { PageTitleBand } from "@four/ui";

/** The red band every inner page opens with. Padding assumes the fixed bar above it. */
export const Default = () => (
  <PageTitleBand title="Deals" tag="Meal deals" tag2="From Rs. 249" lede="Add fries and a drink to any burger from Rs. 249 - here's exactly what the math saves you." titleAs="p" />
);
