import { DealsBand } from "@four/ui";

/** The meal-deals band on cream with three staggered yellow cards. Prices are derived from the menu data. */
export const Default = () => (
  <div className="bg-white">
    <DealsBand />
  </div>
);
