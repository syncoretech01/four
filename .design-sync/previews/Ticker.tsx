import { Ticker } from "@four/ui";

/** The footer ticker on its own. Red ground only - white wordmark, yellow outline text. */
export const Default = () => (
  <div className="on-red py-6">
    <Ticker />
  </div>
);
